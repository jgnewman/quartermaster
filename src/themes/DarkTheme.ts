import type { ThemeProps } from "../ThemeProvider"

const Theme: ThemeProps = {
  avatarBorder: "2px solid red",
  avatarIndicatorOnColor: "blue",
  buttonBgColor: "red",
  buttonContentColor: "white",
}

export default Theme